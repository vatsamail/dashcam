# Sky130 PDK

This repo does not vendor the Sky130 PDK. Install it separately and set `PDK_ROOT`.
Recommended: OpenLane/OpenROAD installation.

Required PDK artifacts:
- Standard cell .lib and .lef
- Tech LEF
- DRC/LVS/ERC decks
- SRAM macros (if used)
